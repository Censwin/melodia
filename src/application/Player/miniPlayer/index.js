import React from 'react';
import {getName} from '../../../api/request';
import {MiniPlayerContainer} from './style'

function MiniPlayer(props) {
  return (
    <MiniPlayerContainer>
      
    </MiniPlayerContainer>
  )
}

export default React.memo(MiniPlayer)